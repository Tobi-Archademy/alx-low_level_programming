#include "holberton.h"
/**
 * add - a function used to add two integers.
 *
 * @a: stores the variable of the datatype to be sum up.
 * @b: stores the variable of the datatype to be sum up.
 * Return: no return
 */
int add(int a, int b)
{
int x;
x = a + b;
return (x);
}
